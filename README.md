# formValidation
Live client side form validation as user moves through the input fields providing the useful error message to fill the form correctly.

# tech
+ vanilla `javascript` dynamic interaction and live form validation
+  `html` & `css` structuring and styling to make form looking beutiful
